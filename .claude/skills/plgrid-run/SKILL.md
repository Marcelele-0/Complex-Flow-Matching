---
name: plgrid-run
description: Run Complex Flow Matching training on PLGrid GPU clusters, especially Athena A100 (plgrid-gpu-a100). Use when connecting to PLGrid over SSH, checking grants and storage with hpc-grants/hpc-fs, preparing project storage and environments around the home quota, writing or submitting Slurm jobs, launching multi-GPU DDP training, monitoring or diagnosing a job, or retrieving results.
---

# Run CFM workloads on PLGrid

This is a discovery stub, not the runbook. The runbook is vendor-neutral and lives in
the repository proper so that humans and every agent brand read the same copy:

**Read [`docs/plgrid/SKILL.md`](../../../docs/plgrid/SKILL.md) in full now**, then the
reference it routes you to.

Do not act on PLGrid from this file alone - the operating rules, the Athena-only
scope warning and the storage discipline are all in that document.

To wire the same runbook into Codex, Cursor, Copilot or Gemini, see
[`docs/plgrid/INSTALL.md`](../../../docs/plgrid/INSTALL.md).
