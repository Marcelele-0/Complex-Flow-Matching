run:
  dir: outputs/${hydra:job.name}/${logging.experiment_name}/${now:%Y-%m-%d}_%H:%M
sweep:
  dir: outputs/${hydra:job.name}/${logging.experiment_name}_sweep/${now:%Y-%m-%d}_%H:%M
  subdir: ${hydra.job.num}
