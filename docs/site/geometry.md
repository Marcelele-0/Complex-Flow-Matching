# Geometries

The cylinder is the paper's method; the plane is the baseline it is measured
against. Everything they do not each define is shared by construction, which is
the property the side-by-side experiment rests on.

::: cyfm.manifolds.cylindrical

::: cyfm.manifolds.euclidean

::: cyfm.manifolds.flat

::: cyfm.manifolds.complex_diffusion
