# Network-free experiments

Measurements that need no trained network, and so reproduce from a bare
checkout in seconds. Each returns its numbers rather than printing them.

::: cyfm.experiments.bridge_geometry
